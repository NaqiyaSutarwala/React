import TextField from "@mui/material/TextField";

const Search = () => {
  return (
    <TextField
      style={{ width: "400px", marginRight: "20px" }}
      id="outlined-search"
      label="Search"
      type="search"
    />
  );
};

export default Search;
